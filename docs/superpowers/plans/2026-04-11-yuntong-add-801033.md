# Yuntong Add 801033 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将运通分析页中“特定编码”相关统计从 3 个编码统一扩展为 4 个编码，新增 `801033`，并保证远端变更可精确回滚。

**Architecture:** 维持现有实现结构，不做额外重构；在旧逻辑 `app.py`、新逻辑 `services/new_sales.py` 与模板 `templates/analysis_result.html` 中同步扩展同一组编码。通过远端 Git 新分支隔离本次修改，只提交目标文件，避免把运行时脏文件混入回滚基线。

**Tech Stack:** Python, Flask/Jinja2, gunicorn, Git, SSH

---

### Task 1: 建立远端可回滚基线

**Files:**
- Modify: `/opt/yuntong/.git` 元数据（仅新建分支与提交）

- [ ] **Step 1: 记录当前 Git 状态与基线提交**

Run: `cd /opt/yuntong && git status --short --branch && git rev-parse HEAD`
Expected: 看到当前分支与现有脏文件列表。

- [ ] **Step 2: 新建隔离分支**

Run: `cd /opt/yuntong && git checkout -b codex/add-801033-specific-coding`
Expected: 切到新分支，不影响现有 `dev` 历史。

- [ ] **Step 3: 确认目标文件无外部未提交改动**

Run: `cd /opt/yuntong && git status --short -- app.py services/new_sales.py templates/analysis_result.html`
Expected: 目标文件为空或仅包含本次改动。

### Task 2: 先写失败验证

**Files:**
- Create: `/opt/yuntong/tests/test_specific_codings.py`（若仓库没有 tests 目录则新建）
- Modify: `/opt/yuntong/app.py`
- Modify: `/opt/yuntong/services/new_sales.py`

- [ ] **Step 1: 写一个失败测试，断言 801033 被纳入统计**

```python
from app import summarize_sales_data
from services.new_sales import summarize_sales_data_new

def _rows():
    return [{
        'store_name': '運通藥房 一店',
        'order_number': 'A001',
        'refund_id': '',
        'return_number': '',
        'coding': '801033',
        'quantity': 2,
        'final_total': 500,
        'price': 500,
    }]

def test_old_summary_counts_801033():
    summary = summarize_sales_data(iter(_rows()))
    assert summary['特定编码(800794, 800795, 800796, 801033)的净数量'] == 2


def test_new_summary_counts_801033():
    summary = summarize_sales_data_new(iter(_rows()))
    assert summary['特定编码(800794, 800795, 800796, 801033)的净数量'] == 2
```

- [ ] **Step 2: 运行测试并确认失败**

Run: `cd /opt/yuntong && python3 -m pytest tests/test_specific_codings.py -q`
Expected: 失败，原因是当前逻辑未统计 `801033` 或 key 名未更新。

### Task 3: 最小实现扩容

**Files:**
- Modify: `/opt/yuntong/app.py`
- Modify: `/opt/yuntong/services/new_sales.py`
- Modify: `/opt/yuntong/templates/analysis_result.html`

- [ ] **Step 1: 将编码集合统一扩容为四个编码**

实现要求：
- 精确匹配列表从 `['800794', '800795', '800796']` 改为包含 `801033`
- 前缀匹配 `startswith` 元组同步包含 `801033`
- summary key 与日志文案同步更新为四个编码

- [ ] **Step 2: 保持改动最小，不引入额外重构**

Run: `cd /opt/yuntong && git diff -- app.py services/new_sales.py templates/analysis_result.html`
Expected: diff 只包含四编码扩容和文案同步。

### Task 4: 运行验证并发布到 60000 页面

**Files:**
- Modify: `/opt/yuntong/restart_gunicorn.sh`（仅在必要时检查，不计划修改）

- [ ] **Step 1: 运行目标测试并确认通过**

Run: `cd /opt/yuntong && python3 -m pytest tests/test_specific_codings.py -q`
Expected: PASS

- [ ] **Step 2: 做语法级回归验证**

Run: `cd /opt/yuntong && python3 -m py_compile app.py services/new_sales.py`
Expected: 无输出，退出码 0。

- [ ] **Step 3: 重启 gunicorn 并确认 60000 端口仍监听**

Run: `cd /opt/yuntong && bash restart_gunicorn.sh && ss -ltnp | grep 60000`
Expected: gunicorn 重新拉起，60000 继续监听。

- [ ] **Step 4: 抽样检查页面响应包含新文案**

Run: `curl -s http://127.0.0.1:60000 | grep -n '801033'`
Expected: 页面或渲染结果中能看到四编码文案；若首页不直接渲染结果页，则至少检查模板变更与应用进程正常。

### Task 5: 精确提交回滚点

**Files:**
- Modify: `/opt/yuntong/.git` 元数据

- [ ] **Step 1: 仅暂存目标文件与新增测试**

Run: `cd /opt/yuntong && git add app.py services/new_sales.py templates/analysis_result.html tests/test_specific_codings.py`
Expected: 不包含 `token_info*.json`、`__pycache__`。

- [ ] **Step 2: 提交中文 commit**

Run: `cd /opt/yuntong && git commit -m '支持特定编码新增801033'`
Expected: 生成仅包含本次变更的回滚点。
